package com.review12;

public interface  Weather {

     /*public abstract */ void showWeather();    // even we don't write public abstract
     											// it does automatically when it's interface

}
